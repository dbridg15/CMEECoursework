#1 a) Count how many lines are in each file

wc -l ../Data/fasta/*`

#2 b) Print everything starting from the second line for the E. coli genome

tail -n +2 ../Data/fasta/E.coli.fasta |tr -d "\n"

#3 c) Count the Sequence length of this genome

tail -n +2 ../Data/fasta/E.coli.fasta |tr -d "\n" |  wc -m

#4 d) Count the matches of a particular sequence "ATGC" in the genome of E. coli

tail -n +2 ../Data/fasta/E.coli.fasta |tr -d "\n" |grep -o ATGC | wc -l

#5 e) Compute the AT/GC ratio

ATcount=$(tail -n +2 ../Data/fasta/E.coli.fasta |tr -d "\n" |grep -o "A\|T" | wc -l) && GTcount=$(tail -n +2 ../Data/fasta/E.coli.fasta |tr -d "\n" |grep -o "G\|C" | 20 wc -l) && Ratio= `expr $ATcount / $GTcount` $$ echo $Ratio



ATcount=$(tail -n +2 ../Data/fasta/E.coli.fasta |tr -d "\n" |grep -o "A\|T" | wc -l) && GTcount=$(tail -n +2 ../Data/fasta/E.coli.fasta |tr -d "\n" |grep -o "G\|C" | 20 wc -l) && Ratio= `expr $ATcount / $GTcount` $$ echo $Ratio





































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































# e) Compute the AT/CG ratio
